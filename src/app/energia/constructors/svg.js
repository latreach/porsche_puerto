import  * from 'constants';
export const svg = d3.select("#energia-animation").append("svg")
  .attr("width", diameter)
  .attr("height", diameter)
  .append("g")
  .attr("transform", "translate(" ṕ+
      diameter /2 + "," + diameter/2 + ")");

